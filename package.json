{
  "name": "cloudflare-top200-fixed-subscription",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "node --test tests/*.test.mjs",
    "check": "npm test"
  },
  "devDependencies": {
    "jsdom": "^26.1.0",
    "wrangler": "^4.0.0"
  }
}
